"""Gateway module — provider-neutral model interfaces.

Provider implementations are intentionally not imported eagerly. This keeps
offline tests and alternative model clients independent from optional SDKs.
"""
